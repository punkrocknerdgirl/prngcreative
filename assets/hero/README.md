# Hero art

One file: `assets/hero/hero.jpg`, 1500 × 1000 (3:2). It sits in a framed
box beside the "here is me" list, inside the site width, and shows the
whole picture at every size (the box is 3:2 too, so nothing is cropped).

- JPEG, quality ~85, under ~400KB. It's the largest-contentful-paint element.
- Alt text lives on the `<img>` in `index.html`; update it if the art changes.
